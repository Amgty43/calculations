function sum(){
    //getting a
    let a=prompt("Enter the first value (a):");
    ///getting b
    let b=prompt("Enter the second value (b):");
    //sum
    let total;
    total=Number(a)+Number(b);
    //Display the results
    console.log(total);
}

function mult(){
    let a = prompt("Enter the first value (a):");

let b = prompt("Enter the second value (b):");

let total;
    total=Number(a) * Number(b);

    console.log(total);
}
//Tempature in fahrenheit
function tempature(){
    let a = prompt("enter the value for celius");
    let temperature = (a)* 9/5+32;
    console.log(temperature)};
    



